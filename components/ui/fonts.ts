import { Inter, Poppins, Lobster, Mansalva } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const poppins = Poppins({ weight: ["400", "700"] ,subsets: ["latin"] });

export const lobster = Lobster({ weight: ["400"] ,subsets: ["latin"] });

export const mansalva = Mansalva({ weight: ["400"] ,subsets: ["latin"] });